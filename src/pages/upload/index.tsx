import * as React from 'react';
import Upload from 'src/components/Upload';

export default class PageUpload extends React.PureComponent<any> {

  render () {
    return (
      <Upload />
    );
  }
}
